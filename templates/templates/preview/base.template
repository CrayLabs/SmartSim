{% include "preview/experiment.template" %}
{%- if manifest %}

=== Entity Preview === 

{%- if manifest.dbs %}
    == Orchestrators == 
{% for db in manifest.dbs %}   
    = {% include "preview/orchestrator.template" %}
    =
{%- endfor %}
{%- endif -%}
{%- endif -%}