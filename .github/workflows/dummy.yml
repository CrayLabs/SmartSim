name: just_for_testing

on:
  pull_request:
  push:
    branches:
      - develop

jobs:
  changelog:
    name: check_changelog
    runs-on: ubuntu-latest

    steps:
    - name: checkout
    - uses: actions/checkout@v4

    categories:
      - title: testing
        labels:
          - '*'