name: just_for_testing

on:
  pull_request:
  push:
    branches:
      - develop

jobs:
  changelog:
    categories:
      - title: 🏕 Features
        labels:
          - '*'
        exclude:
          labels:
            - dependencies
      - title: 👒 Dependencies
        labels:
          - dependencies